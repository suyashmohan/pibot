/**
 * Deprecated during the control-plane transition: the pure helper now lives in
 * `@/lib/control/types` (isomorphic). This module is a re-export only.
 */
export {
  sessionProcessStates,
  type SessionProcessState,
} from "../control/types";
